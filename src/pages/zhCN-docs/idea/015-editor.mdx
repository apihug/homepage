---
title: 015 Editor Assistant
description: Menu context to help edit easier.
---

How to use the edit context menu to code smarter and quicker by ApiHug Api design Copilot

Also refer to the [Shortcuts](/zhCN-docs/idea/000-shortcut)

## Add File

1. Select `proto` module's `src>main>proto`
2. Right click
3. Select `Add Proto File`
4. Select right type proto file

<img src={require('@/img/plugin/115_menu_add_file.gif').default.src} alt="ApiHug Add file" width={2334} height={1768} className="rounded-lg" />

## Edit Message

1. Select `option (hope.domain.kind) = BEAN;` kind proto file
2. right click(shortcut), show up `ApiHug` menu context
3. `Add Message`: Add a new Message
4. `Add Field`: Add a primitive type field
5. `Add Field(Reference)`: Add field reference to another proto object(Message/Enum)

<img src={require('@/img/plugin/116_menu_add_message.gif').default.src} alt="ApiHug Add Message" width={2334} height={1768} className="rounded-lg" />

## Edit Service

1. Select `option (hope.domain.kind) = IDL;` kind proto file
2. right click(shortcut), show up `ApiHug` menu context
3. `Add Service`: Add a new Service
4. `Add Method`: Add a new method to Service

<img src={require('@/img/plugin/117_menu_add_service.gif').default.src} alt="ApiHug Add Service" width={2334} height={1768} className="rounded-lg" />

## Edit Enum

1. Select `option (hope.domain.kind) = ENUM;` kind proto file
2. right click(shortcut), show up `ApiHug` menu context
3. `Add Enum`: Add a new Enum
4. `Add Enum Item`: Add a Enum Item

<img src={require('@/img/plugin/118_menu_add_enum.gif').default.src} alt="ApiHug Add Enum" width={2334} height={1768} className="rounded-lg" />

## Edit Error

1. Select `option (hope.domain.kind) = ERROR;` kind proto file
2. right click(shortcut), show up `ApiHug` menu context
3. `Add Error`: Add a new Error
4. `Add Error Item`: Add a Error Item

<img src={require('@/img/plugin/119_menu_add_error.gif').default.src} alt="ApiHug Add Error" width={2334} height={1768} className="rounded-lg" />

## Edit Entity

1. Select `option (hope.domain.kind) = ENTITY;` kind proto file
2. right click(shortcut), show up `ApiHug` menu context
3. `Add Entity`: Add a new Entity
4. `Add Column`: Add a new Column

<img src={require('@/img/plugin/120_menu_add_entity.gif').default.src} alt="ApiHug Add Entity" width={2334} height={1768} className="rounded-lg" />


## Reference

1. [ApiHug101-Bilibili](https://space.bilibili.com/666522636)
2. [ApiHug101-Youtube](https://youtube.com/@ApiHug?si=C1yw0poHA01zbmyj)
